<center>
  <p><span class="attention">※送信内容を確認し、必ず<b>[ 送信する ]</b>ボタンをクリックして送信を完了してください。</span><br />
    ※送信内容を保存する場合はこのページを印刷、または保存してください。</p>

  <% display_round_box(:width => "660px", :white_bg => false) do %>
    <% form_for(@community, :url => admin_community_path(params[:id]), :html => {:method => :delete}) do |f| %>
      <div class="content_box_bg" align="center">
        <%= f.submit('送信する') %>
        <%= f.submit('入力画面へ戻る', :name => "cancel") %>
      </div>
      <div class="confirmation_message">
        「<%= h @community.name %>」 を削除します。よろしいですか？
      </div>
      <div class="content_box_bg" align="center">
        <%= f.submit('送信する') %>
        <%= f.submit('入力画面へ戻る', :name => "cancel") %>
      </div>
    <% end %>
  <% end %>

  <p><span class="attention"><b>※削除の処理には時間がかかる場合がございます。<br />送信後は画面が切り替わるまで、絶対に再送信や更新、ブラウザを閉じないでください。</b></span></p>
</center>
