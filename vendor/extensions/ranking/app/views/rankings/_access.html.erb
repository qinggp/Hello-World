<%
  link = link_to(h(access.user.name), user_path(access.user))
  count = access.count
  rank = access_counter + 1
  if rank == 1
    link_in_image = link_to(display_face_photo(access.user.face_photo, :width => "100"), user_path(access.user))
  end
%>

<%= display_ranking(rank, link, count, link_in_image).untaint %>


