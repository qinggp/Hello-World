<center>
  <%= theme_image_tag mobile_thumbnail_movie_path(@movie) %>
</center>

ﾀｲﾄﾙ:<br/>
<%=h @movie.title %>
<br/>

ｺﾒﾝﾄ:<br/>
<%=hbr @movie.body  %>
<br/>

公開先:<br/>
<%=h @movie.visibility_to_s %>
<br/>

<% if map = static_map_for_mobile(@movie.latitude, @movie.longitude, @movie.zoom) %>
地図:<br/>
<%= theme_image_tag map.to_s %>
<br/>
<% end  %>

<hr size="1" noshade>

<center>
  <%= link_to "[削除]", confirm_before_destroy_movie_path(@movie) %>
  <%= link_to "[編集]", edit_movie_path(@movie) %>
</center>

<%= render :partial => "footer_mobile" %>
