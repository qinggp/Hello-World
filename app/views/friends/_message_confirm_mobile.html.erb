<b>--- <%=h displayed_user.name %>さんをトモダチに紹介する ---</b><br/>

[紹介先]<br />
<% @receivers.each do |r| %>
  <%= h r.name %><br />
<% end %>
<% params[:receiver_ids].each do |id| %>
  <%= hidden_field_tag "receiver_ids[]", id %>
<% end %>

[紹介文]<%= font_coution("※") %><br/>
<%= h @message.body %><br />
<%= f.hidden_field(:body) %>

<%= f.hidden_field :subject %>
<%= hidden_field_tag("id", displayed_user.id) %>
